# Leave empty in development to use Vite proxy to the API (see vite.config.ts).
# For production build against a remote API, set the full origin, e.g.:
# VITE_API_BASE=https://api.example.com

VITE_API_BASE=
